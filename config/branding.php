<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Client Panel Branding
    |--------------------------------------------------------------------------
    |
    | These values can be overridden via the Admin Settings UI (stored in the
    | database settings table and injected into config at runtime).
    |
    */

    // Relative path under /public (e.g. "uploads/branding/panel_icon_abc123.png").
    'icon' => '',

    'auth_hero_title' => 'Control your servers in one place.',

    'auth_hero_tagline' => 'Secure access to deployments, monitoring, and account tools using the same interface style as your dashboard.',
];
